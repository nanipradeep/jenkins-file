pipeline {
    agent any
    environment {
        PATH = "/opt/maven/bin:$PATH"
    }
    stages {
        stage('Hello') {
            steps {
               git "https://github.com/javahometech/myweb.git"
            }
        }
        stage("build code") {
            steps {
              sh "mvn clean package"
            }
        }
    }
}    
